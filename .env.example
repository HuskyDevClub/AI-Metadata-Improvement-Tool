# Backend API URL
# - For local development: http://localhost:8000
# - For Databricks deployment: leave empty (uses relative URLs)
VITE_API_BASE_URL=http://localhost:8000

# Optional: Pre-fill OpenAI configuration in the UI
# These are loaded into form fields, not used for actual API calls (backend handles that)
VITE_AZURE_ENDPOINT=
VITE_AZURE_KEY=
VITE_AZURE_MODEL=

# Comparison Mode: Default models for A/B testing
# These pre-fill the model selectors when comparison mode is enabled
VITE_COMPARISON_MODEL_A=
VITE_COMPARISON_MODEL_B=
VITE_COMPARISON_JUDGE_MODEL=

# Optional: Pre-fill Socrata API token in the UI
# Required for fetching data from Socrata open data portals
VITE_SOCRATA_APP_TOKEN=
